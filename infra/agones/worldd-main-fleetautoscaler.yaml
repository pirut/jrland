apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: worldd-main-autoscaler
spec:
  fleetName: worldd-main
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 2
      maxReplicas: 12
